# Copy this file to .env.local and paste your Anthropic API key after the = sign.
# .env.local is git-ignored (via *.local) — never commit your real key.
# Get a key at https://console.anthropic.com  (API Keys)
ANTHROPIC_API_KEY=
